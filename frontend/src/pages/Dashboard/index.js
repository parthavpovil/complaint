import React from 'react';
import { Container, Typography } from '@mui/material';

function Dashboard() {
  return (
    <Container>
      <Typography variant="h4">Dashboard</Typography>
      {/* We'll add dashboard content later */}
    </Container>
  );
}

export default Dashboard;